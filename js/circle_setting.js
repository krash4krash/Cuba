$(document).ready(function(){
	// WEB DESIGN
	$('#circle90').one('inview', function() {
	    $(this).circliful({
	        percent: 90,
	        foregroundColor: "#32ADE1"
	    });
	});
	// HTML / CSS
	$('#circle75').one('inview', function() {
	    $(this).circliful({
	        percent: 75,
	        foregroundColor: "#CF3C77"
	    });
	});
	// GRAPHIC DESIGN
	$('#circle70').one('inview', function() {
	    $(this).circliful({
	        percent: 70,
	        foregroundColor: "#26BF98"
	    });
	});
	// UI / UX
	$('#circle85').one('inview', function() {
	    $(this).circliful({
	        percent: 85,
	        foregroundColor: "#E46A3F"
	    });
	});
});


